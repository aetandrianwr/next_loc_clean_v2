# Hyperparameter Tuning Config: pointer_v45_geolife_trial02
# Model: pgt, Dataset: geolife, Trial: 2
# Hyperparameters: {'d_model': 64, 'nhead': 4, 'num_layers': 2, 'dim_feedforward': 128, 'dropout': 0.2, 'learning_rate': 0.001, 'weight_decay': 5e-05, 'batch_size': 256, 'label_smoothing': 0.0, 'warmup_epochs': 5, 'trial_idx': 2, 'model_name': 'pgt', 'dataset': 'geolife'}

seed: 42
data:
  data_dir: data/geolife_eps20/processed
  dataset_prefix: geolife_eps20_prev7
  dataset: geolife
  experiment_root: experiments
  num_workers: 0
model:
  d_model: 64
  nhead: 4
  num_layers: 2
  dim_feedforward: 128
  dropout: 0.2
training:
  batch_size: 256
  num_epochs: 50
  learning_rate: 0.001
  weight_decay: 5.0e-05
  label_smoothing: 0.0
  grad_clip: 0.8
  patience: 5
  min_epochs: 8
  warmup_epochs: 5
  use_amp: true
  min_lr: 1.0e-06
