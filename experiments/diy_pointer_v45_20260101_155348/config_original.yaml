# Hyperparameter Tuning Config: pointer_v45_diy_trial09
# Model: pgt, Dataset: diy, Trial: 9
# Hyperparameters: {'d_model': 64, 'nhead': 4, 'num_layers': 2, 'dim_feedforward': 256, 'dropout': 0.2, 'learning_rate': 0.0005, 'weight_decay': 1e-05, 'batch_size': 64, 'label_smoothing': 0.05, 'warmup_epochs': 7, 'trial_idx': 9, 'model_name': 'pgt', 'dataset': 'diy'}

seed: 42
data:
  data_dir: data/diy_eps50/processed
  dataset_prefix: diy_eps50_prev7
  dataset: diy
  experiment_root: experiments
  num_workers: 0
model:
  d_model: 64
  nhead: 4
  num_layers: 2
  dim_feedforward: 256
  dropout: 0.2
training:
  batch_size: 64
  num_epochs: 50
  learning_rate: 0.0005
  weight_decay: 1.0e-05
  label_smoothing: 0.05
  grad_clip: 0.8
  patience: 5
  min_epochs: 8
  warmup_epochs: 7
  use_amp: true
  min_lr: 1.0e-06
