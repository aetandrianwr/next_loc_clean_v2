# Hyperparameter Tuning Config: pointer_v45_diy_trial12
# Model: pgt, Dataset: diy, Trial: 12
# Hyperparameters: {'d_model': 64, 'nhead': 2, 'num_layers': 2, 'dim_feedforward': 256, 'dropout': 0.25, 'learning_rate': 0.0003, 'weight_decay': 0.0001, 'batch_size': 64, 'label_smoothing': 0.01, 'warmup_epochs': 5, 'trial_idx': 12, 'model_name': 'pgt', 'dataset': 'diy'}

seed: 42
data:
  data_dir: data/diy_eps50/processed
  dataset_prefix: diy_eps50_prev7
  dataset: diy
  experiment_root: experiments
  num_workers: 0
model:
  d_model: 64
  nhead: 2
  num_layers: 2
  dim_feedforward: 256
  dropout: 0.25
training:
  batch_size: 64
  num_epochs: 50
  learning_rate: 0.0003
  weight_decay: 0.0001
  label_smoothing: 0.01
  grad_clip: 0.8
  patience: 5
  min_epochs: 8
  warmup_epochs: 5
  use_amp: true
  min_lr: 1.0e-06
