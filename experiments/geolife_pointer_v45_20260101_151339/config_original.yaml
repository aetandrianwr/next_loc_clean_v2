# Hyperparameter Tuning Config: pointer_v45_geolife_trial14
# Model: pointer_v45, Dataset: geolife, Trial: 14
# Hyperparameters: {'d_model': 128, 'nhead': 4, 'num_layers': 3, 'dim_feedforward': 256, 'dropout': 0.1, 'learning_rate': 0.001, 'weight_decay': 0.001, 'batch_size': 256, 'label_smoothing': 0.0, 'warmup_epochs': 3, 'trial_idx': 14, 'model_name': 'pointer_v45', 'dataset': 'geolife'}

seed: 42
data:
  data_dir: data/geolife_eps20/processed
  dataset_prefix: geolife_eps20_prev7
  dataset: geolife
  experiment_root: experiments
  num_workers: 0
model:
  d_model: 128
  nhead: 4
  num_layers: 3
  dim_feedforward: 256
  dropout: 0.1
training:
  batch_size: 256
  num_epochs: 50
  learning_rate: 0.001
  weight_decay: 0.001
  label_smoothing: 0.0
  grad_clip: 0.8
  patience: 5
  min_epochs: 8
  warmup_epochs: 3
  use_amp: true
  min_lr: 1.0e-06
