# Hyperparameter Tuning Config: pointer_v45_geolife_trial10
# Model: pointer_v45, Dataset: geolife, Trial: 10
# Hyperparameters: {'d_model': 64, 'nhead': 8, 'num_layers': 4, 'dim_feedforward': 256, 'dropout': 0.25, 'learning_rate': 0.0003, 'weight_decay': 1e-05, 'batch_size': 64, 'label_smoothing': 0.05, 'warmup_epochs': 7, 'trial_idx': 10, 'model_name': 'pointer_v45', 'dataset': 'geolife'}

seed: 42
data:
  data_dir: data/geolife_eps20/processed
  dataset_prefix: geolife_eps20_prev7
  dataset: geolife
  experiment_root: experiments
  num_workers: 0
model:
  d_model: 64
  nhead: 8
  num_layers: 4
  dim_feedforward: 256
  dropout: 0.25
training:
  batch_size: 64
  num_epochs: 50
  learning_rate: 0.0003
  weight_decay: 1.0e-05
  label_smoothing: 0.05
  grad_clip: 0.8
  patience: 5
  min_epochs: 8
  warmup_epochs: 7
  use_amp: true
  min_lr: 1.0e-06
