# Hyperparameter Tuning Config: pointer_v45_geolife_trial18
# Model: pgt, Dataset: geolife, Trial: 18
# Hyperparameters: {'d_model': 128, 'nhead': 2, 'num_layers': 3, 'dim_feedforward': 128, 'dropout': 0.25, 'learning_rate': 0.001, 'weight_decay': 0.015, 'batch_size': 64, 'label_smoothing': 0.05, 'warmup_epochs': 5, 'trial_idx': 18, 'model_name': 'pgt', 'dataset': 'geolife'}

seed: 42
data:
  data_dir: data/geolife_eps20/processed
  dataset_prefix: geolife_eps20_prev7
  dataset: geolife
  experiment_root: experiments
  num_workers: 0
model:
  d_model: 128
  nhead: 2
  num_layers: 3
  dim_feedforward: 128
  dropout: 0.25
training:
  batch_size: 64
  num_epochs: 50
  learning_rate: 0.001
  weight_decay: 0.015
  label_smoothing: 0.05
  grad_clip: 0.8
  patience: 5
  min_epochs: 8
  warmup_epochs: 5
  use_amp: true
  min_lr: 1.0e-06
